class Book {
  constructor(bookInfo) {
    const { title, author, numPages, didRead } = bookInfo;
    this.title = title;
    this.author = author;
    this.numPages = numPages;
    this.didRead = didRead;
  }

  // change read status(toggleRead)
}
